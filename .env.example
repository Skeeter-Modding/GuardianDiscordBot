# Discord Bot Environment Variables
# Copy this file to .env and fill in your actual values
# NEVER commit .env to git - it contains sensitive credentials!

# Discord Bot Token (REQUIRED)
# Get this from Discord Developer Portal > Your App > Bot > Reset Token
DISCORD_TOKEN=your-bot-token-here

# Discord Client ID (REQUIRED)
# Get this from Discord Developer Portal > Your App > OAuth2 > Client Information
DISCORD_CLIENT_ID=your-client-id-here

# Discord OAuth2 Client Secret (REQUIRED for dashboard)
# Get this from Discord Developer Portal > Your App > OAuth2 > Client Secret
DISCORD_CLIENT_SECRET=your-discord-client-secret-here

# Session Secret (REQUIRED for dashboard)
# Generate a random secure string - this protects user sessions
SESSION_SECRET=change-this-to-a-random-secure-string

# Dashboard Token Secret (REQUIRED for secure token signing)
# IMPORTANT: Generate a strong random string (32+ chars recommended)
# This is used to cryptographically sign dashboard tokens to prevent forgery
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
DASHBOARD_SECRET=change-this-to-a-strong-random-secret-at-least-32-chars

# Dashboard Configuration
DASHBOARD_PORT=3000
DOMAIN=http://localhost:3000

# Database Configuration (Optional - if not set, bot uses config.json values)
# DB_HOST=localhost
# DB_PORT=3306
# DB_USER=your-db-username
# DB_PASSWORD=your-db-password
# DB_NAME=guardianbot

# AI Configuration (Claude API)
# Get your API key from https://console.anthropic.com/
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# AI Settings (Optional)
# AI_ENABLED=true
# AI_DEFAULT_PERSONALITY=guardian